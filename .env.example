# Example environment variables - copy to .env and modify as needed

# URL for the tracks.json file
# In development, this can be a local path like '/homelab/tracks.json'
# In production, use the full URL to the hosted JSON file
VITE_APP_IMPRESSTO_TRACKS_URL=https://impressto.ca/podcasts/public/tracks.json
